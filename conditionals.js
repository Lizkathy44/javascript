// days in a week
let days = 7
//  Declare password variable and give it a value
// use if statements to :
// check if password has space
// to ensure that your password has a length of more than six characters
// console.log something if something is true

let password = 'hello kathy';
if (password.length >=6){
    if(password.indexOf(' ') ===-1){
        console.log('VALID PASSWORD');
    }
        else{
           console.log("INVALID PASSWORD");
        }
    };

    let newPassword = 'hello kathy';
if (password.length >=6 && password.indexOf(' ')=== -1);
    
    
        else{
           console.log("INVALID PASSWORD");
        }
    // logical operators
    let age = 30;
    if (age<6||age>=65){
        console.log('you get in for free');
    }
    else{
        console.log(`you need pay ksh.${50} to eat`);
    }

    // ternary operator

    let javascript = `hard`;

    if (javascript ===`easy`)?k `okay`: `not true`;
    console.log(javascript);i0